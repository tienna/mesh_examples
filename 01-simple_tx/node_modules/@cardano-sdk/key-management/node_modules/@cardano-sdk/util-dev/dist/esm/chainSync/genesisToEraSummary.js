import { Seconds } from '@cardano-sdk/core';
export const genesisToEraSummary = ({ systemStart, epochLength, slotLength }) => ({
    parameters: {
        epochLength,
        slotLength: Seconds.toMilliseconds(slotLength)
    },
    start: {
        slot: 0,
        time: systemStart
    }
});
//# sourceMappingURL=genesisToEraSummary.js.map