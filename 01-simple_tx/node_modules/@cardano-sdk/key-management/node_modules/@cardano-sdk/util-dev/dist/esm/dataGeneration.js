export const generateRandomHexString = (size) => [...Array.from({ length: size })]
    .map(() => Math.floor(Math.random() * 16).toString(16))
    .join('')
    .toLowerCase();
export const generateRandomBigInt = (min, max) => BigInt(Math.floor(Math.random() * (max + 1 - min) + min));
//# sourceMappingURL=dataGeneration.js.map