import { Lovelace, PoolId, VrfVkHex } from '../../Cardano/index.js';
import { HealthCheckResponse } from '../../Provider/index.js';
import { Milliseconds } from '../../util/index.js';
export interface EraSummary {
    parameters: {
        epochLength: number;
        slotLength: Milliseconds;
    };
    start: {
        slot: number;
        time: Date;
    };
}
export declare type StakeDistribution = Map<PoolId, {
    stake: {
        pool: Lovelace;
        supply: Lovelace;
    };
    vrf: VrfVkHex;
}>;
export interface CardanoNode {
    eraSummaries: () => Promise<EraSummary[]>;
    systemStart: () => Promise<Date>;
    stakeDistribution: () => Promise<StakeDistribution>;
    healthCheck(): Promise<HealthCheckResponse>;
}
//# sourceMappingURL=CardanoNode.d.ts.map