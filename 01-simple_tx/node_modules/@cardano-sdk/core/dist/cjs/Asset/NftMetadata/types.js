"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.MediaType = exports.ImageMediaType = exports.Uri = void 0;
const util_1 = require("@cardano-sdk/util");
const Uri = (uri) => {
    if (/^[a-z]+:\/\/.+/.test(uri)) {
        return uri;
    }
    if (uri.startsWith('data:')) {
        return uri;
    }
    if (uri.startsWith('Qm') && uri.length === 46) {
        return `ipfs://${uri}`;
    }
    throw new util_1.InvalidStringError('Expected Uri to start with "[protocol]:", where protocol is usually "https", "ipfs" or "data"');
};
exports.Uri = Uri;
const ImageMediaType = (mediaType) => {
    if (/^image\/.+$/.test(mediaType)) {
        return mediaType;
    }
    throw new util_1.InvalidStringError('Expected media type to be "image/*"');
};
exports.ImageMediaType = ImageMediaType;
const MediaType = (mediaType) => {
    if (/^[a-z]+\/.+$/.test(mediaType)) {
        return mediaType;
    }
    throw new util_1.InvalidStringError('Expected media type to be "*/*"');
};
exports.MediaType = MediaType;
//# sourceMappingURL=types.js.map