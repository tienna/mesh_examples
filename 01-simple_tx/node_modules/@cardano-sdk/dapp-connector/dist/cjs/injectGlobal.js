"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.injectGlobal = void 0;
const injectGlobal = (window, wallet, logger, injectKey) => {
    injectKey = injectKey ?? wallet.name;
    if (!window.cardano) {
        logger.debug({
            module: 'injectWindow',
            wallet: { apiVersion: wallet.apiVersion, icon: wallet.icon, name: wallet.name }
        }, 'Creating cardano global scope');
        window.cardano = {};
    }
    else {
        logger.debug({
            module: 'injectWindow',
            wallet: { apiVersion: wallet.apiVersion, icon: wallet.icon, name: wallet.name }
        }, 'Cardano global scope exists');
    }
    window.cardano[injectKey] = window.cardano[injectKey] || wallet;
    logger.debug({
        module: 'injectWindow',
        wallet: { apiVersion: wallet.apiVersion, icon: wallet.icon, name: wallet.name },
        windowCardano: window.cardano
    }, 'Injected');
};
exports.injectGlobal = injectGlobal;
//# sourceMappingURL=injectGlobal.js.map