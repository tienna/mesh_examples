export const createPersistentAuthenticatorStorage = (storageKey, store) => ({
    async get() {
        const objects = await store.get(storageKey);
        return objects?.[storageKey] || [];
    },
    async set(origins) {
        await store.set({
            [storageKey]: origins
        });
    }
});
//# sourceMappingURL=PersistentAuthenticatorStorage.js.map