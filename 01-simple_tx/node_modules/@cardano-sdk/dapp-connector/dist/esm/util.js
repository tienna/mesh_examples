export const senderOrigin = (sender) => {
    try {
        const { origin } = new URL(sender?.url || 'throw');
        return origin;
    }
    catch {
        return null;
    }
};
//# sourceMappingURL=util.js.map